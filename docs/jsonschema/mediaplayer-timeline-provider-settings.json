{
  "definitions": {
    "positionSourceConfig": {
      "type": "object",
      "title": "Position Source Configuration",
      "description": "Configuration for a video-based position source that drives timeline position",
      "required": ["systemName"],
      "properties": {
        "systemName": {
          "type": "string",
          "title": "System Name",
          "description": "Name of the position source class to instantiate",
          "examples": ["VideoPositionSource"]
        },
        "selector": {
          "type": "string",
          "title": "Selector",
          "description": "CSS selector for the video player element"
        },
        "poster": {
          "type": "string",
          "title": "Poster",
          "description": "Optional poster image URL for video sources"
        }
      },
      "additionalProperties": true
    },
    "containerProviderConfig": {
      "type": "object",
      "title": "Container Provider Configuration",
      "description": "Configuration for a container provider that manages DOM elements",
      "required": ["systemName", "selector"],
      "properties": {
        "systemName": {
          "type": "string",
          "title": "System Name",
          "description": "Name of the container provider class to instantiate",
          "examples": ["DomContainerProvider"]
        },
        "selector": {
          "type": "string",
          "title": "Selector",
          "description": "CSS selector for the container element"
        }
      },
      "additionalProperties": true
    },
    "playlistConfig": {
      "type": "object",
      "title": "Playlist Configuration",
      "description": "Configuration for a playlist that manages collections of items",
      "required": ["systemName", "items", "identifierKey"],
      "properties": {
        "systemName": {
          "type": "string",
          "title": "System Name",
          "description": "Name of the playlist class to instantiate",
          "examples": ["SimplePlaylist"]
        },
        "items": {
          "type": "array",
          "title": "Items",
          "description": "The items in the playlist"
        },
        "identifierKey": {
          "type": "string",
          "title": "Identifier Key",
          "description": "Property key used to identify items",
          "examples": ["uri", "id", "chapterId"]
        }
      },
      "additionalProperties": true
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://rolandzwaga.github.io/eligius/jsonschema/mediaplayer-timeline-provider-settings.json",
  "type": "object",
  "title": "Media Player Timeline Provider Settings",
  "description": "Settings for mediaplayer-type timeline providers using video.js or similar",
  "required": ["positionSource"],
  "properties": {
    "positionSource": {
      "$ref": "#/definitions/positionSourceConfig",
      "description": "Configuration for the video position source (required)"
    },
    "container": {
      "$ref": "#/definitions/containerProviderConfig",
      "description": "Optional container provider configuration"
    },
    "playlist": {
      "$ref": "#/definitions/playlistConfig",
      "description": "Optional playlist configuration for multi-video timelines"
    }
  }
}
