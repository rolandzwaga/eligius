import { LanguageManager } from '../src';

describe('LanguageManager', () => {

    beforeEach(() => {
    });

    it('should create an instance', () => {
        const languageManager = new LanguageManager(null, null, null);
    });
});